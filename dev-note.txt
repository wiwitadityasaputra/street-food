# run
    $ nvm use
    $ pnpm dev

# menu
    Western -> burger, kebab, hotdog, Currywurst, Fish & Chips, Shawarma & Gyros
    Korean -> Tteokbokki, Eomuk, Gimbap, Gisa/Gamja, Twigim
    Chinese -> Jianbing, Roujiamo, Cong You Bing, Xiaolongbao, Baozi
    Indonesian -> Bakso, Mie Ayam, Siomay, Ketoprak, Martabak Telur


CREATE TYPE cuisine_cart_types AS ENUM ('checkbox', 'radio');
SELECT enum_range(NULL::cuisine_cart_types);

CREATE TABLE "cuisine_cart" (
  "id" integer PRIMARY KEY GENERATED ALWAYS AS IDENTITY (sequence name "cuisine_cart_id_seq"),
  "cuisine_id" smallint NOT NULL,
  "cuisine_cart_type" cuisine_cart_types NOT NULL,
  "group" varchar(25) NOT NULL,
  "name" varchar(50) NOT NULL,
  "price" smallint DEFAULT 0 NOT NULL,
  "flag" boolean DEFAULT true NOT NULL
);

ALTER TABLE "cuisine_cart"
ADD CONSTRAINT "cuisine_cart_fk_cuisine_id" FOREIGN KEY ("cuisine_id") REFERENCES "cuisines" ("id");

# burger
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'burger type', 'Big Mac', 50);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'burger type', 'Normal', 0);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'cheese', 'Extra', 10);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'cheese', 'Normal', 0);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'drinks', 'coca cola', 15);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'drinks', 'pepsi', 15);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'drinks', 'ice tea', 10);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'radio', 'drinks', 'mineral water', 5);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'checkbox', 'add ons', 'french fries', 5);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'checkbox', 'add ons', 'extra chili sauce sachet', 2);
INSERT INTO cuisine_cart ("cuisine_id", "cuisine_cart_type", "group", "name", "price") values (1, 'checkbox', 'add ons', 'extra tomato sauce sachet', 2);

cuisine_cart
    id, cuisine_id, 
    cuisine_cart_type,
    group,
    name,
    price
    flag

size
    Large    20
    Medium   15
    Standar  10

spicy level
    hell
    hot
    medium
    mild
    normal

drink
    coca-cola
    pepsi
    ice tea
    mineral water

add on