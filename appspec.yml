version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "taskdef.json"
        LoadBalancerInfo:
          ContainerName: "DeployToEcsFargateContainerName"
          ContainerPort: 3000